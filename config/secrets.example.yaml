# Optional. Only needed if you don't want to use real environment
# variables / OS keychain for secrets. Copy to secrets.yaml (gitignored)
# and fill in. Values here are loaded as environment variables at startup,
# so config.yaml's ${GIT_TOKEN} etc. can resolve them.

GIT_TOKEN: ""   # GitHub PAT with repo read scope, only if using HTTPS remote
